
users = array()

ul
  for user in users
    li= user
  else
    li no users!


users = array('tobi', 'loki')

if users
  ul
    for user in users
      li= user
    else
      li no users!

user = array()

ul
  - each val, key in user
    li #{key}: #{val}
  - else
    li user has no details!

- user[name] = 'tobi'

ul
  each val, key in user
    li #{key}: #{val}
  else
    li user has no details!
